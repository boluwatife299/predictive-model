streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.2.0
numpy>=1.26.0
yfinance>=0.2.40
scipy>=1.13.0
python-dotenv>=1.0.0

# Statistical models
statsmodels>=0.14.0
arch>=6.3.0

# Machine learning
scikit-learn>=1.5.0
xgboost>=2.0.0

# Prophet and LSTM are optional — install locally if you want them:
#   pip install prophet tensorflow-cpu
# They are excluded here because they exceed Streamlit Cloud free-tier
# build limits (cmdstanpy compilation + 1 GB RAM cap).
