# Copy this file to `.streamlit/secrets.toml` (which is git-ignored) and fill in
# your real values. On Streamlit Community Cloud, paste the same keys into
# Settings → Secrets instead of using a file.

# ── Persistence ────────────────────────────────────────────────────────────
# Supabase: Project → Settings → Database → Connection string (URI).
# Use the "Connection pooling" (Transaction) URI for serverless/Streamlit.
# It looks like:
#   postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DATABASE_URL = "postgresql://postgres.xxxx:PASSWORD@aws-0-region.pooler.supabase.com:6543/postgres"

# ── AI layer (daily news & trends) — optional ───────────────────────────────
# Default provider is Gemini (free tier, Google Search grounding).
# Get a key at https://aistudio.google.com/apikey
AI_PROVIDER = "gemini"
GEMINI_API_KEY = ""
# GEMINI_MODEL = "gemini-2.5-flash"   # optional override

# To use Claude instead, set AI_PROVIDER = "anthropic" and fill in:
# ANTHROPIC_API_KEY = ""
# AI_MODEL = "claude-sonnet-4-6"      # optional override
